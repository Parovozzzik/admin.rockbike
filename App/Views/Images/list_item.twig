<tr id="image_{{ image.image_id }}" role="tab">

    {% include '/App/Views/Components/Table/td/link.twig' with {
        'href': '/images/view/' ~ image.image_id,
        'value': image.image_id,
    } %}

    {% include '/App/Views/Components/Table/td/image.twig' with {
        'value': image.path,
        'created_at': image.created_at
    } %}

    {% include '/App/Views/Components/Table/td/status.twig' with {
        'value': image.visible,
        'text_enabled': 'Visible',
        'text_disabled': 'Not visible',
    } %}

    {% include '/App/Views/Components/Table/td/status.twig' with {
        'value': image.deleted,
        'text_enabled': 'Deleted',
        'text_disabled': 'Not deleted',
    } %}

    {% set buttons = [
        {'icon': 'hs-admin-pencil', 'href': '/images/edit/' ~ image.image_id},
        {'icon': 'hs-admin-trash', 'href': '/images/delete/' ~ image.image_id, 'onclick': 'Are you sure you want to delete this item?'},
    ] %}

    {% include '/App/Views/Components/Table/td/buttons.twig' with {
        'buttons': buttons
    } %}

</tr>